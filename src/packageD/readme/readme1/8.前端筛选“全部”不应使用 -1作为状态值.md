我直接给你一个**结论先行 + 精准定位**，不绕弯子。

------

## ✅ 一句话结论（你现在这个页面 100% 的问题）

> **问题不在模板，不在 uni-app，也不在接口**
>
> 👉 **问题在前端 `status`的类型设计**
>
> 你把 **“全部（-1）”当成一个合法状态值传给后端**
>
> 但后端 `OrderPageQuery.status`已经是：

```
private OrderStatusEnum status;
```

**Jackson 无法把 `-1`转成枚举，直接炸。**

------

## 🔴 精准定位：问题就在这 3 个地方

------

### ❌ 1️⃣ 这里（致命）

```
const activeStatus = ref(-1); // -1表示全部
```

❌ `-1`**不是 OrderStatusEnum**

❌ Jackson 默认会把数字当 **enum ordinal**

❌ `OrderStatusEnum.values()[-1]`→ 越界异常

👉 **这就是你日志里的根因**

------

### ❌ 2️⃣ 这里（把 -1 直接塞给后端）

```
const orderStatusList = computed(() => [
  { value: -1, label: "全部", class: "all", count: 0 },
  ...
]);
const params = {
  status: activeStatus.value, // ❌ 这里把 -1 传出去了
};
```

------

### ❌ 3️⃣ 这里（点击“全部”又补了一刀）

```
const onStatusClick = (status: number) => {
  if (activeStatus.value === status) {
    activeStatus.value = -1; // ❌ 再一次强制用 -1
  } else {
    activeStatus.value = status;
  }
  loadOrders(true);
};
```

------

## ✅ 正确、规范、一次性的修复方式（照着改）

### ✅ 第一步：`activeStatus`改成 `null`

```
const activeStatus = ref<null | string>(null); // null = 全部
```

------

### ✅ 第二步：“全部”用 `null`

```
const orderStatusList = computed(() => [
  { value: null, label: "全部", class: "all", count: 0 },
  ...ORDER_STATUS_VALUES.map((status) => ({
    value: status, // 这里是 "PAID" / "UNPAID" 等字符串 ✅
    label: OrderStatusLabel[status],
    class: OrderStatusClass[status],
    count: 0,
  })),
]);
```

------

### ✅ 第三步：**只有非 null 才传 status**

```
const getFilterParams = () => {
  const params: any = {
    startTime: dateRange.value.start + " 00:00:00",
    endTime: dateRange.value.end + " 23:59:59",
    ...advancedFilter.value,
  };

  if (activeStatus.value !== null) {
    params.status = activeStatus.value;
  }

  return params;
};
```

------

### ✅ 第四步：点击“全部”时设为 `null`

```
const onStatusClick = (status: string | null) => {
  if (activeStatus.value === status) {
    activeStatus.value = null; // ✅ 取消筛选
  } else {
    activeStatus.value = status;
  }
  loadOrders(true);
};
```

------

## ✅ 为什么这样 **一定不会再报错**

| 场景   | 实际传参      | 后端       |
| ------ | ------------- | ---------- |
| 全部   | ❌ 不传 status | ✅ 不筛选   |
| 待发货 | `"PAID"`      | ✅ 枚举匹配 |
| 已发货 | `"SHIPPED"`   | ✅ 枚举匹配 |

✅ 不再有 `-1`

✅ 不再有 Jackson ordinal 反序列化

✅ 和昨天 / 今天所有异常一次性闭环

------

## ⚠️ 顺带提醒你一个“隐藏雷点”

你现在这里：

```
ship: order.status === OrderStatusEnum.PAID,
```

✅ **是对的**

✅ 前提是：

```
export enum OrderStatusEnum {
  UNPAID = "UNPAID",
  PAID = "PAID",
  SHIPPED = "SHIPPED",
}
```

如果还是数字枚举，这里迟早再炸一次。

------

## ✅ 最终结论（你可以直接贴给后端）

> 前端筛选“全部”不应使用 `-1`作为状态值
>
> 后端 `OrderPageQuery.status`为 `OrderStatusEnum`
>
> 前端约定：
>
> - 
>
>   不传 `status`= 查询全部
>
> - 
>
>   传值只能是枚举字符串（`PAID / SHIPPED`）
>
> 已按此规范调整前端代码，接口可直接恢复。

------

如果你愿意，我可以 **直接帮你把这一整份 `<script setup>`改成“完全符合后端枚举规范的最终版”**，你复制就能用，不用再踩第二次坑。