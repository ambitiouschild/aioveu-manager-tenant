就是一句话的事，我给你拆开说：

---

`onStatusClick` 是点状态栏（待付款/待发货/已完成等）时触发的。

每次点状态，有两种可能：

### 情况一：你点的是当前已经高亮的状态

比如现在"待发货"是高亮的，你又点了一下"待发货"——意思是取消筛选，回到"全部"。

```ts
if (activeStatus.value === status) {
  // 取消筛选
```

这时候要判断：你当前是什么模式？

• 如果当前是 `pending` 模式（从"全部待处理"进来的）→ 只把 `activeStatus` 清掉，让列表回到"所有待处理"混合数据，`filterMode` 不动，picker 继续显示"全部时间"

• 如果是普通模式 → 切回 `all`，picker 恢复正常日期


---

### 情况二：你点的是另一个状态

比如当前高亮"待发货"，你点了"待付款"——意思是切换到待付款。

```ts
} else {
```

同样要判断当前模式：

• 如果当前是 `pending` 模式 → 只换 `activeStatus`，`filterMode` 保持 `pending` 不动。这样 picker 继续显示"全部时间"，但列表只返回你选的那个状态

• 如果是普通模式 → 正常切到 `single` 模式


---

用大白话说就是：

> 如果你是从"全部待处理"进来的，那你在状态栏之间来回切的时候，别把 `filterMode` 改掉，不然 picker 就不显示"全部时间"了。只有点其他快捷筛选（今日/昨日）时才重置模式。

之前的问题就是不管三七二十一，点状态栏就强制切成 `single`，所以 picker 的"全部时间"就消失了。

---

要不要我帮你把这段逻辑再简化一下？其实可以写得更短更清晰。