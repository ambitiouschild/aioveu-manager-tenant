**我看到了！** 你的原始数据确实包含了二级分类（在 `children`数组中），但你的 `buildTree`函数**只处理了扁平化数组，没有处理嵌套的 `children`结构**。

------

## 🔍 问题根源

你的数据是这样的结构：

```
[
  {
    id: 195,
    name: "大家电",
    parentId: 0,
    level: 1,
    children: [  // 👈 二级分类在 children 数组中
      { id: 198, name: "厨房电器", parentId: 195, level: 2 },
      { id: 199, name: "清洁电器", parentId: 195, level: 2 },
      // ...
    ]
  },
  // ...
]
```

但你的 `buildTree`函数假设**数据是扁平的**，通过 `parentId`递归构建树。实际上，**数据已经是树形结构了**，你不需要用 `buildTree`函数。

------

## ✅ 最小修复方案

**删除 `buildTree`函数，直接使用原始数据**：

```
const handleQuery = async () => {
  loading.value = true
  try {
    const response = await PmsCategoryAPI.getListCategories({})

    console.log('📊 原始数据:', response)

    if (Array.isArray(response)) {
      rawCategoryList.value = response
      
      // ✅ 修复：直接使用原始数据，不需要 buildTree
      // 因为数据已经是树形结构
      const processedData = response.map(item => ({
        ...item,
        level: item.level || 1,  // 保持原有层级
        expanded: false,  // 默认不展开
        // 如果有 children，处理子节点
        children: item.children ? item.children.map(child => ({
          ...child,
          level: child.level || 2,
          expanded: false,
          children: []  // 假设没有三级分类
        })) : []
      }))
      
      treeData.value = [{
        id: 0,
        name: '全部分类',
        parentId: 0,
        level: 0,
        expanded: true,
        children: processedData
      }]

      console.log('🌳 处理后的树结构:')
      console.log(JSON.stringify(treeData.value, null, 2))
      
      // 检查数据
      const checkNode = (node: any, indent: string = '') => {
        console.log(`${indent}${node.name} (id:${node.id}, level:${node.level}): 有 ${node.children?.length || 0} 个子节点`)
        if (node.children) {
          node.children.forEach((child: any) => checkNode(child, indent + '  '))
        }
      }
      checkNode(treeData.value[0])

    } else {
      console.error('返回数据格式不正确:', response)
      treeData.value = []
    }
  } catch (error) {
    console.error('查询分类数据失败:', error)
  } finally {
    loading.value = false
  }
}
```

------

## 🔧 或者，修改 `buildTree`来处理嵌套结构：

如果你还想用 `buildTree`，需要修改它以处理嵌套的 `children`：

```
/**
 * ✅ 修复：处理嵌套的树形数据
 */
function processNestedTree(list: CategoryNode[]): CategoryNode[] {
  return list.map(item => {
    // 如果有 children，递归处理
    const children = item.children ? processNestedTree(item.children) : []
    
    return {
      ...item,
      level: item.level || 0,
      expanded: false,
      children: children.length > 0 ? children : []
    }
  })
}
```

然后在 `handleQuery`中：

```
const processedData = processNestedTree(response)
treeData.value = [{
  id: 0,
  name: '全部分类',
  parentId: 0,
  level: 0,
  expanded: true,
  children: processedData
}]
```

------

## 🎯 最简单的方法

**直接删除 `buildTree`调用，用原始数据**：

```
// ✅ 最简单：直接使用原始数据
treeData.value = [{
  id: 0,
  name: '全部分类',
  parentId: 0,
  level: 0,
  expanded: true,
  children: response.map(item => ({
    ...item,
    expanded: false
  }))
}]
```

------

## 📊 你的数据流应该是：

```
后端返回（已经是树） → 添加根节点 → FlatTree显示
```

**不需要 `buildTree`转换**，因为数据已经是树形结构了。

------

## ✅ 最终修复步骤

在你的 `handleQuery`函数中，**只修改这一部分**：

```
if (Array.isArray(response)) {
  rawCategoryList.value = response
  
  // ✅ 直接使用原始数据（已经是树形结构）
  treeData.value = [{
    id: 0,
    name: '全部分类',
    parentId: 0,
    level: 0,
    expanded: true,
    children: response.map(item => ({
      ...item,
      expanded: false
    }))
  }]
  
  console.log('🌳 最终树结构:', treeData.value[0])
}
```

------

## ⚡ 一句话总结

**你的数据已经是树形结构（有 `children`字段），不需要用 `buildTree`函数转换。直接使用原始数据，添加根节点即可。**

试试这个最小修改，然后看 `FlatTree`是否能正确显示二级分类。