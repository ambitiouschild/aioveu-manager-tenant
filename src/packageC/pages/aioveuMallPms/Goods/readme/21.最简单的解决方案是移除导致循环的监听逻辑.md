您说得对！最简单的解决方案是**移除导致循环的监听逻辑**。只需要修改几个关键地方：

## 最小改动方案

### 1. 移除 `immediate: true`和自动重置

```
// ❌ 删除或修改 watchCategoryChange 函数
// 不要使用 immediate: true
// 不要自动重置属性列表
```

### 2. 修改 `onMounted`中的初始化

```
onMounted(() => {
  console.log("🔄 商品属性组件挂载");

  // ❌ 移除这个自动重置
  // if (!goodsInfo.value.attrList || goodsInfo.value.attrList.length === 0) {
  //   resetAttributeList();  // ❌ 这里会触发循环
  // }

  // ✅ 改为简单的检查
  console.log("📊 当前属性数据:", goodsInfo.value.attrList);
  
  // ❌ 移除这个会触发循环的监听
  // watchCategoryChange();

  // 只计算表格高度
  nextTick(() => {
    calculateTableHeight();
  });
});
```

### 3. 简化 `resetAttributeList`函数

```
/**
 * 重置属性列表
 */
const resetAttributeList = (): void => {
  // ✅ 只修改本地数据，不触发 computed 更新
  const newGoodsInfo = { 
    ...props.goodsInfo,  // 使用原始 props
    attrList: [new GoodsAttribute()]
  };
  
  // ✅ 直接触发 emit，不通过 computed
  emit("update:goodsInfo", newGoodsInfo);
  
  errors.value = [{}];
  console.log("🔄 重置属性列表（手动触发）");
};
```

### 4. 修复 `calculateTableHeight`中的 this 问题

```
import { getCurrentInstance } from "vue";

// 获取组件实例
const instance = getCurrentInstance();

/**
 * 计算表格高度
 */
const calculateTableHeight = () => {
  if (!instance) {
    console.warn("无法获取组件实例");
    return;
  }
  
  const query = uni.createSelectorQuery().in(instance);
  query
    .select(".component-container")
    .boundingClientRect((data: any) => {
      if (data) {
        const windowHeight = systemInfo.windowHeight;
        const containerTop = data.top;
        const footerHeight = 100;
        const headerHeight = 100;
        tableHeight.value = windowHeight - containerTop - footerHeight - headerHeight - 20;
      }
    })
    .exec();
};
```

### 5. 修改 `handleAttributeInput`避免通过 computed

```
/**
 * 处理属性输入
 */
const handleAttributeInput = (e: any, index: number, field: "name" | "value"): void => {
  const value = e.detail ? e.detail.value : e.target.value;
  
  // ✅ 创建新的数组，不直接修改 computed
  const newAttrList = [...(props.goodsInfo?.attrList || [])];
  if (!newAttrList[index]) {
    newAttrList[index] = new GoodsAttribute();
  }
  newAttrList[index] = { ...newAttrList[index], [field]: value };
  
  // ✅ 直接触发更新，不经过 computed
  const newGoodsInfo = { 
    ...props.goodsInfo, 
    attrList: newAttrList 
  };
  emit("update:goodsInfo", newGoodsInfo);
  
  // 清除错误
  if (errors.value[index]) {
    const newErrors = [...errors.value];
    delete newErrors[index][field];
    errors.value = newErrors;
  }
};
```

## 完整的最小改动版本

```
<script setup lang="ts">
import { ref, computed, watch, onMounted, onUnmounted, nextTick, getCurrentInstance } from "vue";

// 获取组件实例
const instance = getCurrentInstance();

// ... 其他代码保持不变 ...

// ✅ 修改计算属性，只读取不触发循环
const goodsInfo = computed({
  get: (): GoodsInfoData => {
    const data = props.goodsInfo || {};
    return {
      id: data.id,
      categoryId: data.categoryId,
      // ✅ 简化，不自动添加空行
      attrList: Array.isArray(data.attrList) 
        ? data.attrList.map((attr) => new GoodsAttribute(attr))
        : [],
    };
  },
  set: (value: GoodsInfoData) => {
    // ✅ 添加日志，但不做其他处理
    console.log("📤 触发商品信息更新");
    emit("update:goodsInfo", value);
  },
});

// ✅ 移除 watchCategoryChange 函数的调用
// 或者在调用时去掉 immediate: true
const setupSafeWatch = () => {
  watch(
    () => goodsInfo.value.categoryId,
    async (newCategoryId) => {
      console.log("🔄 分类ID变化:", newCategoryId);
      
      // ✅ 编辑模式不自动加载
      if (goodsInfo.value.id && goodsInfo.value.attrList && goodsInfo.value.attrList.length > 0) {
        console.log("📝 编辑模式，保持原有属性");
        return;
      }
      
      // 新增模式才加载
      if (newCategoryId) {
        console.log("🆕 新增模式，加载分类属性");
        await loadCategoryAttributes(newCategoryId);
      }
      // ❌ 不要在这里调用 resetAttributeList()
    },
    {
      immediate: false, // ✅ 重要：不要立即执行
      deep: false,
    }
  );
};

onMounted(() => {
  console.log("🔄 商品属性组件挂载");
  
  // ✅ 只初始化错误数组
  const attrList = goodsInfo.value.attrList || [];
  errors.value = attrList.length > 0 
    ? attrList.map(() => ({}))
    : [{}];
  
  // ✅ 设置安全的监听
  setupSafeWatch();
  
  // 计算表格高度
  nextTick(() => {
    calculateTableHeight();
  });
});

// ✅ 修改 calculateTableHeight
const calculateTableHeight = () => {
  if (!instance) {
    console.warn("无法获取组件实例");
    return;
  }
  
  const query = uni.createSelectorQuery().in(instance);
  query
    .select(".component-container")
    .boundingClientRect((data: any) => {
      if (data) {
        const windowHeight = systemInfo.windowHeight;
        const containerTop = data.top;
        const footerHeight = 100;
        const headerHeight = 100;
        tableHeight.value = windowHeight - containerTop - footerHeight - headerHeight - 20;
      }
    })
    .exec();
};
</script>
```

## 总结

**最小改动 = 3个关键修改**：

1. **移除 `immediate: true`** - 避免立即执行触发循环
2. **修改 `onMounted`** - 不自动调用 `resetAttributeList`
3. **修复 `calculateTableHeight`的 this** - 使用 `getCurrentInstance()`

这样就不会在组件加载时自动触发重置，从而避免无限循环。