好，这个文件**现在离“最终正确形态”只差一步了**。

我直接给你一个 **「只改该改的地方、最小侵入版」** 的处理结果 ✅

------

# ✅ 你这份代码现在的问题定位

你现在的问题是 **职责混乱**：

| 地方                     | 现在在干嘛                   |
| ------------------------ | ---------------------------- |
| `requestInterceptor`     | ✅ token / clientId           |
| `responseInterceptor`    | ✅ 401 / A0230                |
| `request.success`        | ❌ **不该处理 TOKEN_INVALID** |
| `handleTokenExpiredSync` | ✅ 统一跳转                   |

👉 **TOKEN_INVALID 的逻辑应该“完全交给响应拦截器”**

------

# ✅ 我帮你「最终处理版」（只改关键位置）

## ✅ 一、响应拦截器（✅ 接管 TOKEN_INVALID）

```
const responseInterceptor = (response: any) => {
  const resData = response.data;

  // HTTP 层认证失败
  if (response.statusCode === 401) {
    console.error("❌ 认证失败，状态码 401");
    handleTokenExpiredSync();
    return Promise.reject({
      code: 'AUTH_ERROR',
      message: '认证失败',
      response,
    });
  }

  // 业务层 token 失效
  if (
    resData.code === ResultCodeEnum.TOKEN_INVALID ||
    resData.code === 'A0230'
  ) {
    handleTokenExpiredSync();
    return Promise.reject({
      code: resData.code,
      message: resData.msg || 'token失效',
    });
  }

  return resData;
};
```

✅ **这里统一兜底**

✅ **不再让 request.success 操心 token**

------

## ✅ 二、request.success（✅ 只认拦截器结果）

### ❌ 你现在的 request.success 里这段 **必须删掉**

```
else if (resData.code === ResultCodeEnum.TOKEN_INVALID) {
  setTimeout(() => {
    handleTokenExpiredSync();
  }, 0);

  reject({
    code: resData.code,
    message: resData.msg || "token失效",
  });
}
```

### ✅ 改成这样（干净版）

```
success: (response) => {
  const responseWithConfig = {
    ...response,
    config: processedConfig,
  };

  try {
    const interceptorResult = responseInterceptor(responseWithConfig);

    if (interceptorResult && interceptorResult.then) {
      interceptorResult.then(resolve).catch(reject);
      return;
    }

    const resData = response.data as ResponseData<T>;

    if (resData.code === ResultCodeEnum.SUCCESS) {
      resolve(resData.data);
    } else {
      uni.showToast({
        title: resData.msg || "业务处理失败",
        icon: "none",
      });
      reject({
        code: resData.code,
        message: resData.msg || "业务处理失败",
      });
    }
  } catch (error) {
    reject(error);
  }
},
```

✅ **TOKEN_INVALID 完全由拦截器接管**

✅ **request 只负责 SUCCESS / 普通业务错误**

------

## ✅ 三、handleTokenExpiredSync（✅ 保留，不用动）

```
let isTokenExpiredHandling = false;

function handleTokenExpiredSync() {
  if (isTokenExpiredHandling) return;
  isTokenExpiredHandling = true;

  clearAll();

  setTimeout(() => {
    uni.showToast({
      title: "登录已过期，请重新登录",
      icon: "none",
    });
  }, 100);

  setTimeout(() => {
    uni.reLaunch({
      url: "/packageA/pages/login/login",
    });
    isTokenExpiredHandling = false;
  }, 1500);
}
```

✅ 并发安全

✅ UI 不阻塞 Promise

------

# ✅ 最终结论（你可以放心用）

| 层级                   | 是否处理 TOKEN_INVALID |
| ---------------------- | ---------------------- |
| requestInterceptor     | ❌                      |
| responseInterceptor    | ✅ **唯一正确位置**     |
| request.success        | ❌                      |
| handleTokenExpiredSync | ✅ 工具函数             |

------

## ✅ 一句话总结

> ✅ **TOKEN_INVALID 的逻辑必须有**
>
> ✅ **它只能、也必须放在响应拦截器里**
>
> ❌ **request.success 里不要再碰 token**

------

如果你愿意，下一步我可以帮你：

- 

  ✅ 抽一个 `baseRequest`

- 

  ✅ 把 `request`/ `request2`合并

- 

  ✅ 把 `NOT_LOGGED_IN / TOKEN_INVALID / 401`全部统一

你现在已经走在 **“企业级 request 架构”** 的路上了 👍