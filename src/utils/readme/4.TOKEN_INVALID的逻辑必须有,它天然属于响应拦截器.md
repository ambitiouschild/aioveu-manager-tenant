✅ **完全正确，而且你已经抓到“拦截器设计的核心原则”了。**

我直接给你一个**“可以当作规范”的结论**👇

------

## ✅ 最终正确结论（重点）

> ✅ **`TOKEN_INVALID`的逻辑「应该有」**
>
> ✅ **它就应该放在「响应拦截器」里处理**
>
> ❌ **不应该再散落在 request 里**

------

## 一、为什么 `TOKEN_INVALID`属于「响应拦截器」？

| 原因           | 说明               |
| -------------- | ------------------ |
| 它是后端返回的 | ✅ 请求已经发出     |
| 它是业务状态码 | ✅ 不是 HTTP 状态码 |
| 它影响全局状态 | ✅ 登录态失效       |
| 它应该统一处理 | ✅ 所有接口共用     |

👉 **这就是响应拦截器的“本职工作”**

------

## 二、你现在这套代码里，最标准的写法 ✅

### ✅ 响应拦截器中统一处理

```
const responseInterceptor = (response: any) => {
  const resData = response.data;

  // HTTP 层
  if (response.statusCode === 401) {
    handleTokenExpiredSync();
    return Promise.reject({
      code: 'AUTH_ERROR',
      message: '认证失败',
    });
  }

  // 业务层
  if (resData.code === ResultCodeEnum.TOKEN_INVALID) {
    handleTokenExpiredSync();
    return Promise.reject({
      code: resData.code,
      message: resData.msg || 'token失效',
    });
  }

  return resData;
};
```

------

## 三、request 里只干一件事 ✅

```
const interceptorResult = responseInterceptor(responseWithConfig);

if (interceptorResult && interceptorResult.then) {
  interceptorResult.then(resolve).catch(reject);
  return;
}
```

✅ **request 不关心 token 是否失效**

✅ **只负责：把响应交给拦截器**

------

## 四、你现在的架构已经是对的 ✅

| 层级       | 职责                         |
| ---------- | ---------------------------- |
| 请求拦截器 | ✅ token 注入 / NOT_LOGGED_IN |
| 响应拦截器 | ✅ TOKEN_INVALID / 401        |
| request    | ✅ 发请求 + 接结果            |

👉 **这是企业级标准写法**

------

## 五、一句话总结（你可以收藏）

> ✅ **`TOKEN_INVALID`的逻辑必须有**
>
> ✅ **它天然属于响应拦截器**
>
> ❌ **不应该再出现在 request 的成功回调里**

------

如果你愿意，我可以帮你把这套 **request + 拦截器**

整理成一个 **「可复用的 request 模板」**，

以后新项目 **直接 copy 就能用** 👍